## 2026-03-19

- Implemented booking API routes plus server validation and JSON helpers.
- Backend assumption: route-owned booking storage currently uses an in-memory Map behind `src/server/http/bookings.ts` until the separate persistence layer is available.
